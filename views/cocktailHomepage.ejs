<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Cocktail's Hub</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <link href="/css/cocktailHomepage.css" rel="stylesheet">
</head>
<body>
    <nav class="navbar bg-body-tertiary">
        <div class="container-fluid">
          <a class="navbar-brand">Cocktails Hub</a>
          <form class="d-flex" role="search">
            <input class="form-control me-2" type="search" placeholder="Search" aria-label="Search">
            <button class="btn btn-outline-success" type="submit">Search</button>
          </form>
        </div>
      </nav>
    <div class="container text-center">
        <div class="row">
          <div class="ad col">
            Column
          </div>
          <div class="drinks col-8">
            <div class="container-fluid">
                <div class="row">
                    <div class="col">
                        <% if (locals.drinks) { %>
                            <% for( let index = 0; index < drinks.length; index++ ) { %>
                                <div class="card">
                                    <img src=<%= drinks[index].image %> class="card-img-top" alt="Picture Unavailable">
                                    <div class="card-body">
                                        <p class="card-text"><%=drinks[index].title %></p>
                                    </div>
                                </div>
                            <% } %>
                        <% } %>
                    </div>
                </div>
            </div>
          </div>
          <div class="ad col">
            Column
          </div>
        </div>
    </div>
    
    
    
</body>
</html>